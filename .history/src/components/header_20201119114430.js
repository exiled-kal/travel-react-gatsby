import { Link } from "gatsby"
import React from "react"

const Header = () => (
  
)

Header.propTypes = {
  siteTitle: PropTypes.string,
}

Header.defaultProps = {
  siteTitle: ``,
}

export default Header
